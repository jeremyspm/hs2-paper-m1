/* Exam-case pack for Module 1 — the three starred Case Study Booklet A cases the final
   exam draws on (2 lymph node removal, 3 asthma, 4 panic attack). Scenario and questions
   are HERS verbatim; the steps come from HER 2024 model-answers deck where it answers,
   and say so per question in `src`. Empty until authored. */
export const CASES = [];
