/* Extra ACCEPTED answers for typed blanks where her Canvas key marks a plainly
   correct spelling wrong. Her key stays the displayed answer. Matched on question
   id + blank index + her correct answer; build.mjs fails on one that matches nothing.
   Module 1: none yet — add here when a sitting turns one up. */
export const OVERRIDES = [];
